# 🧪 GitHub Pages Priority Test

This page is part of the `github-repository` folder inside `0x476974.github.io`.

However, there is also a separate repository named `github-repository` with GitHub Pages enabled.

This test is created to check which GitHub Pages source has higher priority when both locations exist:

- 🌐 `0x476974.github.io/github-repository`
- 📦 `github-repository` repository GitHub Pages site

The purpose is to understand GitHub Pages behavior and source priority.